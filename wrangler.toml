name = "turkiye-haritam"
compatibility_date = "2024-09-01"

# Pure static Cloudflare Pages site. `npm run build` copies the servable files
# into dist/ (keeping node_modules out); Pages serves that directory.
pages_build_output_dir = "dist"
